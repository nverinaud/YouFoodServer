<tr>
    <td><%= link_to(menu.name, menu_path(menu)) %></td>
    <td><%= menu.products.count %></td>
    <td>
        <% if menu.schedules.any? %>
            Semaine
            <% menu.schedules.each do |schedule| %>
                <%= " #{schedule.week}" %>
                <% if schedule != menu.schedules.last %>
                    ,
                <% end %>
            <% end %>
        <% else %>
            Aucune planification
        <% end %>
    </td>
</tr>